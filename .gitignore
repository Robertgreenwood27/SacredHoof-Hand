/node_modules
/.next
/out
/build
.DS_Store
*.pem
npm-debug.log*
.env
.env*.local
.vercel
next-env.d.ts
*.tsbuildinfo

# Backups of env files and anything else — .env*.local does NOT match
# names like ".env.local.bak", which is how a secret nearly got committed.
*.bak
.env*.local.*
